<section class="section"><div class="wrap wrap--narrow" data-reveal>
  <div class="section-head"><h1 class="h-display">Reset password</h1></div>
  {% form 'reset_customer_password' %}
    {{ form.errors | default_errors }}
    <div class="flow">
      <input class="variant-pill" style="width:100%" type="password" name="customer[password]" placeholder="New password">
      <input class="variant-pill" style="width:100%" type="password" name="customer[password_confirmation]" placeholder="Confirm password">
      <button class="btn btn--block" type="submit">Reset</button>
    </div>
  {% endform %}
</div></section>
